<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:usesCleartextTraffic="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:replace="android:allowBackup,android:fullBackupContent,android:usesCleartextTraffic">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            WHY: Android WebView storage is process-scoped when a data-directory
            suffix is used. Keeping private browsing in its own process prevents
            Chromium cookies/storage/cache from sharing the normal profile.
        -->
        <activity
            android:name=".privatebrowser.PrivateBrowserActivity"
            android:exported="false"
            android:process=":private"
            android:excludeFromRecents="false"
            android:launchMode="singleTop"
            android:label="Private Browser"
            android:theme="@style/Theme.MyApplication" />
    </application>

</manifest>
